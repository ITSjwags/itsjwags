---
interface Props {
  label: string;
  class?: string;
}

const { label, class: className = '' } = Astro.props;
---

<h2 class={`font-mono text-[14px] font-medium text-accent ${className}`}>&gt; {label}</h2>
