---
interface Props {
  label: string;
  class?: string;
}

const { label, class: className = '' } = Astro.props;
---

<h2 class={`text-label font-mono font-semibold text-accent ${className}`}>&gt; {label}</h2>
