export const RESUME_PATH = '/resume/JonathanWagoner-Resume.pdf';

// Base.astro's pre-paint theme script is `is:inline` (must run synchronously,
// blocking, before first paint) and can't import this module — its literal
// copy of this key must be kept in sync by hand.
export const THEME_STORAGE_KEY = 'jw-theme';
